export type position = {
    x: number,
    y: number,
}

export type size = {
    w: number,
    h: number,
}

export type quad = [
    number,
    number,
];

export type iconKeyAndTexts = {
    icon: string,
    key: string,
    sound: string,
    texts: string[],
}